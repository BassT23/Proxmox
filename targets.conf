# Optional inventory for future non-Proxmox targets.
#
# This file is intentionally not required for the existing Proxmox workflow.
# Add one section per target.  Keep only reachability information here;
# operating system, distribution, package manager and update method should be
# detected by a future target implementation.
#
# Example:
# [raspi]
# host=192.168.10.50
# transport=ssh
# user=basst
