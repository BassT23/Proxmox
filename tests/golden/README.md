# Updater output golden files

These fixtures describe the stable user-facing vocabulary and layout for the
main check/update scenarios. Timestamps, job IDs, temporary paths, and host
runtime details are deliberately fixed or omitted. The shell regression test
renders the status/check cases through the real CLI and uses the existing
status/mail renderer tests for update-result coverage.
